app:
  name: SecureVista Pro
  version: "3.0"
  codename: NOVA x SENTINEL
  demo_mode: true

database:
  path: securevista.db

risk:
  high_threshold: 0.65
  critical_threshold: 0.85
  correlation_window_minutes: 15

physical:
  loitering_seconds: 30
  night_brightness_threshold: 60
  qr_registered_hashes:
    MERCHANT_ZONE_01: QR_SHARMA_STORE_V1

blockchain:
  network: polygon_amoy
  dry_run: true

alerts:
  twilio_dry_run: true
